package com.example.syncsmart.data

// Intentionally empty: the app doesn't sign in. The Login screen (see
// ui/screens/auth/LoginScreen.kt) is UI only and its button just moves on to
// Dashboard — no Firebase Auth call, no "firebase-auth" Gradle dependency.
// This file is a placeholder in case that changes later.
